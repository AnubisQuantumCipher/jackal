{
  "schema": "jackal-hermes-plugin-v1",
  "plugin_id": "jackal_range_bound",
  "version": "v1.7.3",
  "description": "JACKAL evidence kernel: the preserved v1.7.2 formal, exact, weaker, and claim lanes; four manifest-pinned domain-pack tools for structural source facts and caller-criterion decisions; and three Anubis Safe program-evidence tools. The program tools close a strict anubis.program-evidence.v3 tree, bind caller-pinned source/compiler/artifact/policy identities, replay approved Z3 UNSAT plus independent RUP proofs, and emit only verified-program-evidence or verified-program-receipt. Their inventory-safe-v1 policy deliberately does not claim independent policy-construct totality, source-to-VC proof, SMT-to-CNF proof, source-native refinement, runtime behavior, or universal language soundness. Domain-pack assurance and consequence ceilings remain separate: test existence stays informational and decision ranking stays decision-boundary. No lane is promoted to formal by this additive surface.",
  "bundle_files": [
    "server.py",
    "bundle_hash.py",
    "jackal_hermes",
    "tools.json"
  ],
  "bundle_identity_schema": "jackal-hermes-runtime-bundle-v2",
  "runtime_files": {
    "plugin/bundle_hash.py": [
      "bundle_hash.py"
    ],
    "plugin/jackal_hermes": [
      "jackal_hermes"
    ],
    "plugin/server.py": [
      "server.py"
    ],
    "plugin/tools.json": [
      "tools.json"
    ],
    "runtime/coverage_inventory.py": [
      "../../tools/coverage_inventory.py",
      "../../coverage_inventory.py"
    ],
    "runtime/formal_coverage_inventory.json": [
      "../../release/coverage/formal_coverage_inventory.json",
      "../../formal_coverage_inventory.json"
    ],
    "runtime/range_proof_identity.json": [
      "../../release/evidence/range_proof_identity_v172.json",
      "../../release/evidence/range_proof_identity.json",
      "../../range_proof_identity.json"
    ],
    "runtime/archival_range_proof_identity.json": [
      "../../release/evidence/range_proof_identity.json",
      "../../evidence/range_proof_identity_v1.json"
    ],
    "runtime/gaussian_proof_identity.json": [
      "../../release/evidence/gaussian_proof_identity.json",
      "../../gaussian_proof_identity.json"
    ],
    "runtime/formal_receipt.py": [
      "../../tools/formal_receipt.py",
      "../../formal_receipt.py"
    ],
    "runtime/formal_status_gate.py": [
      "../../tools/formal_status_gate.py",
      "../../formal_status_gate.py"
    ],
    "runtime/gaussian_certificate.py": [
      "../../tools/gaussian_certificate.py",
      "../../gaussian_certificate.py"
    ],
    "runtime/gaussian_release.py": [
      "../../tools/gaussian_release.py",
      "../../gaussian_release.py"
    ],
    "runtime/isolated_entry.py": [
      "../../tools/isolated_entry.py",
      "../../isolated_entry.py"
    ],
    "runtime/receipt_verify.py": [
      "../../tools/receipt_verify.py",
      "../../receipt_verify.py"
    ],
    "runtime/release_validate.py": [
      "../../tests/release_validate.py",
      "../../release_validate.py"
    ]
    ,
    "runtime/sqrt_rat_producer.py": [
      "../../tools/sqrt_rat_producer.py",
      "../../sqrt_rat_producer.py"
    ],
    "runtime/exp_rat_producer.py": [
      "../../tools/exp_rat_producer.py",
      "../../exp_rat_producer.py"
    ],
    "runtime/ln_rat_producer.py": [
      "../../tools/ln_rat_producer.py",
      "../../ln_rat_producer.py"
    ],
    "runtime/sin_rat_producer.py": [
      "../../tools/sin_rat_producer.py",
      "../../sin_rat_producer.py"
    ],
    "runtime/atan_rat_producer.py": [
      "../../tools/atan_rat_producer.py",
      "../../atan_rat_producer.py"
    ],
    "runtime/tanh_rat_producer.py": [
      "../../tools/tanh_rat_producer.py",
      "../../tanh_rat_producer.py"
    ],
    "runtime/exact_verify.py": [
      "../../tools/exact_verify.py",
      "../../exact_verify.py"
    ],
    "runtime/int_cert_producer.py": [
      "../../tools/int_cert_producer.py",
      "../../int_cert_producer.py"
    ],
    "runtime/int_cert_release.py": [
      "../../tools/int_cert_release.py",
      "../../int_cert_release.py"
    ],
    "runtime/int_cert_proof_identity.json": [
      "../../release/evidence/int_cert_proof_identity_v172.json",
      "../../release/evidence/int_cert_proof_identity.json",
      "../../int_cert_proof_identity.json"
    ],
    "runtime/archival_int_cert_proof_identity.json": [
      "../../release/evidence/int_cert_proof_identity.json",
      "../../evidence/int_cert_proof_identity_v1.json"
    ],
    "runtime/claim_kernel.py": [
      "../../tools/claim_kernel.py",
      "../../claim_kernel.py"
    ],
    "runtime/claim_router.py": [
      "../../tools/claim_router.py",
      "../../claim_router.py"
    ],
    "runtime/claim_bundle_verify.py": [
      "../../tools/claim_bundle_verify.py",
      "../../claim_bundle_verify.py"
    ],
    "runtime/inference_registry_v1.json": [
      "../../release/claim/inference_registry_v1.json",
      "../../inference_registry_v1.json"
    ],
    "runtime/unit_registry_v1.json": [
      "../../release/claim/unit_registry_v1.json",
      "../../unit_registry_v1.json"
    ],
    "runtime/anubis_program_verify.py": [
      "../../tools/anubis_program_verify.py"
    ],
    "runtime/anubis_program_policy.json": [
      "../../release/program/inventory_safe_v1.json",
      "../../program/inventory_safe_v1.json"
    ]
  },
  "tools": [
    {
      "name": "jackal_range_bound",
      "description": "Emit a formal-bounded receipt with the certificate embedded (or refuse).",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Anubis expression in the single variable x, restricted to the certified fragment."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced ℚ (e.g. '1', '2/3')."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo)."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "receipt": "jackal-formal-receipt-v1 object on success",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_gaussian_integral",
      "description": "Emit a zero-libm formal-bounded Gaussian integral receipt, rerun the pinned checker, or refuse without downgrade.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Exact canonical exp(-A*(x-mu)^2) expression; all other expressions refuse."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Integration lower bound as a canonical rational."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Integration upper bound as a canonical rational."},
        "tolerance":  {"type": "string", "required": true,
                       "help": "Maximum enclosure width as a positive canonical rational."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "checker_rerun": "ACCEPT on success",
        "receipt": "jackal-formal-receipt-v1 object on success",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_integrate_bound_cert",
      "description": "Emit a request-bound certified composed definite-integral formal receipt (v1.7.2): the untrusted exact-rational producer mirrors the engine's adaptive subdivision, the pinned Lean-proved jackal_int_cert_check binds the exact raw expression/bounds/tolerance and re-checks the whole subdivision-tree certificate (theorem int_cert_sound), and the receipt is independently re-verified before returning. Request-unbound v1.7.0 receipts are revoked. Certified fragment: num/var/neg/add/sub/mul/div/pow(0..4096)/sin/cos/abs in x. Everything else refuses. The weaker float lane jackal_integrate_bound stays status=bounded and is NOT this tool.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Integrand in the single variable x, restricted to the certified fragment (num/var/neg/add/sub/mul/div/pow/sin/cos/abs)."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Integration lower bound as an integer or reduced canonical rational (e.g. '0', '2/3')."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Integration upper bound as a canonical rational, strictly above input_lo."},
        "tolerance":  {"type": "string", "required": true,
                       "help": "Maximum enclosure width as a positive canonical rational (e.g. '1/100')."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "checker_rerun": "ACCEPT on success",
        "receipt": "jackal-formal-receipt-v1 object (variant int_cert) on success",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_verify_receipt",
      "description": "Re-run the matching pinned Lean-proved checker over an embedded certificate using a closed epoch/variant registry: current v1.7.2 range/rational and request-bound int_cert, replay-only v1.5.0 range/rational, or Gaussian v1.5.0. Request-unbound v1.7.0 int_cert receipts refuse.",
      "arguments": {
        "receipt": {"type": "object", "required": true,
                    "help": "A jackal-formal-receipt-v1 JSON document."},
        "expected_release_epoch": {"type": "string", "required": true,
                    "help": "Caller-authorized release epoch, not copied from the receipt."},
        "expected_command": {"type": "string", "required": true,
                    "help": "Caller-authorized operation: range-bound-cert or integrate."},
        "expected_expression": {"type": "string", "required": true,
                    "help": "Exact raw expression supplied by the caller."},
        "expected_input_lo": {"type": "string", "required": true,
                    "help": "Exact raw lower-bound token supplied by the caller."},
        "expected_input_hi": {"type": "string", "required": true,
                    "help": "Exact raw upper-bound token supplied by the caller."},
        "expected_tolerance": {"type": "string", "required": false,
                    "help": "Exact raw tolerance token; required for Gaussian and int_cert receipts and forbidden for range receipts."}
      },
      "returns": {
        "status": "verified | refused",
        "verdict": "ACCEPT on success",
        "receipt_digest_sha256": "recomputed outer digest",
        "certificate_sha256": "recomputed certificate SHA-256",
        "checker_sha256": "pinned checker binary hash",
        "evaluator_sha256": "pinned evaluator binary hash",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_sqrt_rat_bound",
      "description": "Emit a formal-bounded pure-Q sqrt(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'sqrt(x)' on a canonical rational interval; every other expression refuses without downgrade. v1.4.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'sqrt(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational (e.g. '2', '1/4'); must be >= 0."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo)."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "sqrt_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing sqrt(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_exp_rat_bound",
      "description": "Emit a formal-bounded pure-Q exp(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'exp(x)' on a canonical rational interval [lo, hi] (general-sign since v1.5.0); every other expression refuses without downgrade. v1.4.1 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'exp(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational; any sign (general-sign since v1.5.0)."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo); the producer picks a Taylor degree with 2*hi <= n+1."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "exp_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing exp(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_ln_rat_bound",
      "description": "Emit a formal-bounded pure-Q ln(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'ln(x)' on a canonical rational interval with lo > 0; every other expression refuses without downgrade. v1.5.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'ln(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational; must be > 0."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo)."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "ln_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing ln(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_sin_rat_bound",
      "description": "Emit a formal-bounded pure-Q sin(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'sin(x)' on a canonical rational interval whose midpoint m satisfies |m| <= 1; every other expression refuses without downgrade. v1.5.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'sin(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo); midpoint (lo+hi)/2 must satisfy |m| <= 1."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "sin_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing sin(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_cos_rat_bound",
      "description": "Emit a formal-bounded pure-Q cos(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'cos(x)' on a canonical rational interval whose midpoint m satisfies |m| <= 1; every other expression refuses without downgrade. Shares the sin_rat producer (--op cos). v1.5.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'cos(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo); midpoint (lo+hi)/2 must satisfy |m| <= 1."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "cos_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing cos(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_atan_rat_bound",
      "description": "Emit a formal-bounded pure-Q atan(x) enclosure via the untrusted producer + Lean-proved checker. NO libm on the proof-decision path. Admits ONLY the exact form 'atan(x)' on a canonical rational interval; every other expression refuses without downgrade. v1.5.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal 'atan(x)' after whitespace normalization."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo)."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "atan_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing atan(x) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_tanh_rat_bound",
      "description": "Emit a formal-bounded pure-Q tanh enclosure via the untrusted composite producer + Lean-proved checker. NO libm on the proof-decision path. tanh is not an engine grammar token: admits ONLY the literal composite expression '1-2/(exp(2*x)+1)' (= tanh(x) mathematically) on a canonical rational interval with |lo|,|hi| <= 20; every other expression refuses without downgrade. v1.5.0 fragment extension.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Must equal '1-2/(exp(2*x)+1)' after whitespace normalization (the frozen tanh-defining composite)."},
        "input_lo":   {"type": "string", "required": true,
                       "help": "Interval lower bound as an integer or reduced rational; |lo| <= 20."},
        "input_hi":   {"type": "string", "required": true,
                       "help": "Interval upper bound (canonical rational, hi >= lo); |hi| <= 20."}
      },
      "returns": {
        "status": "formal-bounded | refused",
        "variant": "tanh_rat",
        "enclosure": "[lo, hi] canonical rationals enclosing 1-2/(exp(2*x)+1) over the input interval",
        "certificate_b64": "base64 of the exact canonical certificate bytes the checker accepted",
        "certificate_sha256": "SHA-256 of the certificate bytes",
        "checker_verdict": "ACCEPT on success",
        "identities": "{producer_sha256, checker_sha256, plugin_sha256}",
        "theorem_id": "request_bound_certified_release",
        "release_epoch": "v1.7.2",
        "reason":  "stable refusal class on refusal",
        "detail":  "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_exact",
      "description": "Exact big-rational arithmetic (status=exact). NOT formal: computationally exact, outside the Lean certificate chain.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Integers, decimals, + - * / ^ (integer exponents), parentheses. Everything else refuses."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "parsed engine key=value map (exact=, approx=, parsed=)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_evaluate",
      "description": "IEEE f64 expression evaluation (status=estimated). A plain number, honestly labeled: no bound, no proof.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Full expression grammar incl. transcendental functions and constants."}
      },
      "returns": {
        "status": "estimated | refused",
        "engine_output": "verbatim engine stdout (the f64 value)"
      }
    },
    {
      "name": "jackal_diff",
      "description": "Symbolic d/dx with numeric self-verification (status=checked). Sampled agreement is a check, not a proof of identity.",
      "arguments": {
        "expression": {"type": "string", "required": true,
                       "help": "Expression in x; non-differentiable functions fail closed."}
      },
      "returns": {
        "status": "checked | refused",
        "fields": "check=, points=, max-rel-dev=, assurance=",
        "engine_output": "derivative line + status line, verbatim"
      }
    },
    {
      "name": "jackal_integrate",
      "description": "Fixed-grid Simpson + Richardson error estimate (status=estimated). Grid-limited: NOT a bound.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Integrand in x."},
        "input_lo":   {"type": "string", "required": true, "help": "Lower limit."},
        "input_hi":   {"type": "string", "required": true, "help": "Upper limit."},
        "panels":     {"type": "string", "required": true, "help": "Panel count (e.g. '200')."}
      },
      "returns": {
        "status": "estimated | refused",
        "fields": "integral=, richardson-error-estimate=, assurance="
      }
    },
    {
      "name": "jackal_integrate_adaptive",
      "description": "Adaptive Simpson with refusal semantics (status=estimated). Refuses when unconverged; agreement is still not a bound.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Integrand in x."},
        "input_lo":   {"type": "string", "required": true, "help": "Lower limit."},
        "input_hi":   {"type": "string", "required": true, "help": "Upper limit."},
        "tolerance":  {"type": "string", "required": true, "help": "Local tolerance (e.g. '1e-9')."}
      },
      "returns": {
        "status": "estimated | refused",
        "fields": "integral=, achieved-error-estimate=, assurance="
      }
    },
    {
      "name": "jackal_integrate_bound",
      "description": "Certified interval enclosure of an integral (status=bounded). CONDITIONAL on the stated f64/libm rounding model; implementation campaign-tested, NOT mechanized. Never labeled formal. For a Lean-checked formal-bounded composed enclosure over the certified fragment, use jackal_integrate_bound_cert instead.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Integrand in x."},
        "input_lo":   {"type": "string", "required": true, "help": "Lower limit."},
        "input_hi":   {"type": "string", "required": true, "help": "Upper limit."},
        "tolerance":  {"type": "string", "required": true, "help": "Max enclosure width (e.g. '1e-6')."}
      },
      "returns": {
        "status": "bounded | refused",
        "fields": "integral-enclosure=, width=, assurance=certified-bound(...)"
      }
    },
    {
      "name": "jackal_solve",
      "description": "Bisection root with residual + first-order conditioning diagnostics (status=estimated).",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "f(x) whose root is sought."},
        "input_lo":   {"type": "string", "required": true, "help": "Bracket lower bound (sign change required)."},
        "input_hi":   {"type": "string", "required": true, "help": "Bracket upper bound."}
      },
      "returns": {
        "status": "estimated | refused",
        "fields": "root=, residual=, condition-amplification=, root-error-estimate=, assurance="
      }
    },
    {
      "name": "jackal_canon",
      "description": "Canonical s-expression + SHA-256 of any parsed expression (status=exact). NOT formal: exact computation outside the Lean certificate chain; no exact-cert emitted.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Any expression the engine grammar parses."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "canonical=, sha256=",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_poly_canon",
      "description": "Dense Q[x] canonical form, degree <= 64 (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Polynomial expression in x within the poly fragment."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "degree=, coeffs=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_poly_eq",
      "description": "Decidable polynomial identity over Q[x] (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "lhs": {"type": "string", "required": true, "help": "Left polynomial expression in x."},
        "rhs": {"type": "string", "required": true, "help": "Right polynomial expression in x."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "equal=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_poly_gcd",
      "description": "Monic polynomial gcd over Q[x] via Euclid (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "lhs": {"type": "string", "required": true, "help": "Left polynomial expression in x."},
        "rhs": {"type": "string", "required": true, "help": "Right polynomial expression in x."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "gcd=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_ratfunc_canon",
      "description": "Rational-function canonical form P/Q, gcd-reduced, monic denominator, explicit denominator-nonzero side condition (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Rational-function expression in x within the ratfunc fragment."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "num=, den=, side-condition=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_roots_isolate",
      "description": "Sturm-sequence isolation of all distinct real roots (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Polynomial expression in x within the poly fragment."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "distinct-real-roots=, intervals=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_alg_sign",
      "description": "Exact sign of a Q[x] polynomial at a rational point (status=exact). NOT formal; no exact-cert emitted.",
      "arguments": {
        "expression": {"type": "string", "required": true, "help": "Polynomial expression in x within the poly fragment."},
        "point": {"type": "string", "required": true, "help": "Rational evaluation point (e.g. '3/2')."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "sign=",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_alg_cmp",
      "description": "Order decision between two isolated real algebraic numbers (status=exact). NOT formal; no exact-cert emitted.",
      "arguments": {
        "p":  {"type": "string", "required": true, "help": "First defining polynomial in x."},
        "a1": {"type": "string", "required": true, "help": "First isolating interval lower bound (rational)."},
        "b1": {"type": "string", "required": true, "help": "First isolating interval upper bound (rational)."},
        "q":  {"type": "string", "required": true, "help": "Second defining polynomial in x."},
        "a2": {"type": "string", "required": true, "help": "Second isolating interval lower bound (rational)."},
        "b2": {"type": "string", "required": true, "help": "Second isolating interval upper bound (rational)."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "order=",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_xgcd",
      "description": "Extended gcd with Bezout certificate (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "a": {"type": "string", "required": true, "help": "First integer."},
        "b": {"type": "string", "required": true, "help": "Second integer."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "g=, u=, v=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_mod_pow",
      "description": "Modular exponentiation via square-and-multiply (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "base": {"type": "string", "required": true, "help": "Base integer."},
        "exp":  {"type": "string", "required": true, "help": "Nonnegative exponent integer."},
        "mod":  {"type": "string", "required": true, "help": "Modulus integer >= 1."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "r=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_mod_inv",
      "description": "Modular inverse with product certificate (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "a": {"type": "string", "required": true, "help": "Integer to invert."},
        "m": {"type": "string", "required": true, "help": "Modulus integer >= 2; gcd(a, m) must be 1."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "inv=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_crt",
      "description": "Chinese remainder reconstruction over pairwise-coprime moduli, up to 16 pairs (status=exact). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "args": {"type": "string", "required": true,
                 "help": "Space-separated residue/modulus pairs: 'r1 m1 r2 m2 [...]' (e.g. '2 3 3 5 2 7')."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "x=, M=, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_divides",
      "description": "Exact divisibility decision (status=exact). NOT formal; no exact-cert emitted.",
      "arguments": {
        "a": {"type": "string", "required": true, "help": "Candidate divisor integer."},
        "b": {"type": "string", "required": true, "help": "Dividend integer."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "divides=",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_prime_cert",
      "description": "Pratt primality certificate or composite divisor witness, budgeted and fail-closed (status=exact; n <= 10^60, Pratt tree depth <= 64, nodes <= 512). Emits a jackal-exact-cert-v1 certificate independently re-checkable by tools/exact_verify.py. NOT formal.",
      "arguments": {
        "n": {"type": "string", "required": true, "help": "Integer >= 2, at most 61 digits."}
      },
      "returns": {
        "status": "exact | refused",
        "fields": "verdict= (prime|composite), n=, divisor= or method=pratt, exact_cert (raw jackal-exact-cert-v1 JSON string)",
        "engine_output": "verbatim engine stdout",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_claim",
      "description": "Compile a structured jackal-claim-request-v1 into a canonical, content-addressed jackal-claim-bundle-v1 evidence graph through the deterministic policy router. Routes through exact, current/archival range-family, Gaussian, request-bound composed-integral, machine, and unit lanes; emits a route trace naming candidates and refusal reasons; and refuses rather than silently downgrading (allow_fallback defaults false). The bundle is independently replayable via jackal_verify_bundle.",
      "arguments": {
        "request": {
          "type": "object",
          "required": true,
          "help": "jackal-claim-request-v1 object: {schema, steps:[{id, op, ...}], root, policy?, nonce?, emitted_at_unix?, max_age_seconds?, expires_at_unix?}. Step ops: input, exact, enclose, gaussian, integrate_cert, machine, interval_add/sub/mul/div, threshold, decision, convert, and, model, passthrough, attach."
        }
      },
      "returns": {
        "status": "ok | refused",
        "root": "root claim-node id (SHA-256 of canonical node bytes)",
        "bundle_digest_sha256": "SHA-256 of canonical bundle bytes",
        "rendering": "deterministic {token, permitted_text} — never a bare VERIFIED badge",
        "route_trace": "per-step candidate lanes with selected/refused status and reasons",
        "bundle": "the full jackal-claim-bundle-v1 object",
        "reason": "stable refusal class on refusal"
      }
    },
    {
      "name": "jackal_verify_bundle",
      "description": "Independently replay a jackal-claim-bundle-v1 against caller-pinned expectations. The standalone dependency-free verifier recomputes every canonical byte and hash, revalidates the DAG, selects only the closed current/archival range, Gaussian, or current request-bound int-cert checker/proof tuple, re-runs embedded evidence, recomputes machine/unit mathematics, re-evaluates every inference rule and assurance-axis propagation, enforces consequence-class floors and policy, and recomputes the deterministic rendering. Returns verified | refused | indeterminate with exact reasons — never a generic green badge.",
      "arguments": {
        "bundle": {
          "type": "object",
          "required": true,
          "help": "The jackal-claim-bundle-v1 object to replay."
        },
        "expected_release_epoch": {
          "type": "string",
          "required": true,
          "help": "Caller-pinned epoch (e.g. 'v1.6.0'); never copied from the bundle."
        },
        "expected_policy_sha256": {
          "type": "string",
          "required": true,
          "help": "Caller-pinned SHA-256 of the canonical policy bytes."
        },
        "expected_root_proposition": {
          "type": "object",
          "required": true,
          "help": "Caller-pinned canonical root proposition IR object."
        },
        "verification_time_unix": {
          "type": "string",
          "required": true,
          "help": "Caller-supplied verification time (unix seconds) for freshness/expiry checks."
        },
        "expected_nonce": {
          "type": "string",
          "required": false,
          "help": "Caller nonce; the bundle root must bind exactly this nonce."
        }
      },
      "returns": {
        "status": "verified | refused | indeterminate",
        "report": "full verifier output lines (axes, artifact flags, freshness, assumptions, residual non-claims, decision binding, rendering token, permitted text)",
        "reason": "stable refusal class on refusal/indeterminate",
        "detail": "short human-readable detail"
      }
    },
    {
      "name": "jackal_test_exists",
      "description": "Domain pack jackal.programming.source, operation programming.source.test_exists.v1, routed through pack-route. States one byte-exact STRUCTURAL fact: a declaration-shaped occurrence of `symbol` exists at `declaration_line` in a file whose content hash is exactly `file_sha256`, and the file contains exactly `declaration_count` such occurrences. The engine validates the canonical FORM; the manifest-pinned independent checker tools/test_exists_verify.py then recomputes every claimed field from the real bytes on disk and only an ACCEPT verdict returns success, so a misstated hash, line or count refuses instead of minting a certificate. ASSURANCE ceiling exact; CONSEQUENCE ceiling informational, and the second does not rise with the first: a test-exists-cert is NEVER evidence that the code under test is correct, that the test executes, that it is collected by any runner, or that it asserts anything at all. NOT formal: no Lean checker and no theorem are involved. Do not cite this certificate in support of a correctness claim.",
      "arguments": {
        "file_path":         {"type": "string", "required": true,
                              "help": "Repository-relative path token; absolute paths and parent traversal refuse `prog-path`."},
        "file_sha256":       {"type": "string", "required": true,
                              "help": "Exactly 64 lowercase hex characters; the checker recomputes it from the file bytes."},
        "symbol":            {"type": "string", "required": true,
                              "help": "Identifier of the declaration; a non-identifier refuses `prog-symbol`."},
        "declaration_line":  {"type": "string", "required": true,
                              "help": "1-based line of the declaration as a canonical positive integer."},
        "declaration_count": {"type": "string", "required": true,
                              "help": "Total declaration-shaped occurrences of `symbol` in the file; `0` refuses `prog-absent`."}
      },
      "returns": {
        "status": "structural-exact | refused",
        "assurance_ceiling": "exact",
        "consequence_ceiling": "informational",
        "checker_rerun": "ACCEPT on success (tools/test_exists_verify.py, digest pinned by the pack manifest)",
        "certificate": "the verbatim jackal-test-exists-cert-v1 envelope line",
        "identities": "{registry_file_sha256, registry_digest_sha256, pack_verifier_sha256, pack_manifest_sha256, evaluator_sha256, checker_sha256, plugin_sha256}",
        "non_claims": "the operation's pinned nonclaims plus the informational consequence ceiling; registry, verifier, and checker identities are release-manifest pinned",
        "reason": "stable refusal class on refusal (prog-path, prog-hex64, prog-symbol, prog-uint, prog-absent, pack-request-arity, checker-rejected, ...)",
        "detail": "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_claim_cites_test",
      "description": "Domain pack jackal.programming.source, operation programming.source.claim_cites_test.v1, routed through pack-route. Resolves a citation: `claim_text` occurs verbatim in the document at `doc_path`/`doc_sha256`, and `symbol` has a declaration-shaped occurrence in the cited test file at `test_path`/`test_sha256`. Resolution is all this establishes. It does NOT establish that the cited test covers, exercises or supports the claim — the cited test may check something entirely different, which is the exact defect this operation exists to bound rather than hide. The manifest-pinned tools/test_exists_verify.py re-runs over the certificate and only ACCEPT returns success, so a claim text absent from the document or a dangling citation refuses. ASSURANCE ceiling exact; CONSEQUENCE ceiling informational, and the second does not rise with the first. NOT formal: no Lean checker and no theorem are involved.",
      "arguments": {
        "doc_path":    {"type": "string", "required": true,
                        "help": "Repository-relative path of the document making the claim."},
        "doc_sha256":  {"type": "string", "required": true,
                        "help": "Exactly 64 lowercase hex characters for the document bytes."},
        "claim_text":  {"type": "string", "required": true,
                        "help": "The claim sentence, verbatim, 1..2048 bytes; it must occur in the document."},
        "test_path":   {"type": "string", "required": true,
                        "help": "Repository-relative path of the cited test file."},
        "test_sha256": {"type": "string", "required": true,
                        "help": "Exactly 64 lowercase hex characters for the cited test file bytes."},
        "symbol":      {"type": "string", "required": true,
                        "help": "Identifier the citation resolves to inside the cited test file."}
      },
      "returns": {
        "status": "structural-exact | refused",
        "assurance_ceiling": "exact",
        "consequence_ceiling": "informational",
        "checker_rerun": "ACCEPT on success (tools/test_exists_verify.py, digest pinned by the pack manifest)",
        "certificate": "the verbatim jackal-test-exists-cert-v1 envelope line (kind claim-cites-test)",
        "identities": "{registry_file_sha256, registry_digest_sha256, pack_verifier_sha256, pack_manifest_sha256, evaluator_sha256, checker_sha256, plugin_sha256}",
        "non_claims": "includes cited_test_may_check_something_else and citation_resolution_is_not_coverage",
        "reason": "stable refusal class on refusal (prog-path, prog-hex64, prog-symbol, prog-text, pack-request-arity, checker-rejected, ...)",
        "detail": "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_decision_rank",
      "description": "Domain pack jackal.decision.matrix, operation decision.matrix.rank.v1, routed through pack-route. Orders 2..6 labelled options by a caller-declared numeric criterion and emits a jackal-decision-cert-v1 naming the selected option, the runner-up and the exact margin; the manifest-pinned tools/decision_verify.py recomputes the whole ordering from the certificate's own option values and only ACCEPT returns success. Criterion admissibility is decided by the engine against a fixed word list, so a bare value judgment refuses `decision-value-judgment`; a top-two tie refuses `decision-margin-zero`. ASSURANCE ceiling exact; CONSEQUENCE ceiling decision-boundary. The arithmetic is exact and the choice of criterion is not: the declared criterion and the declared option values remain the caller's, this result is not a claim that the criterion is the right one, the values are not measurements, and the margin is not a confidence interval. For the closed-unit lane that additionally requires a declared unit, use jackal_decision_rank_v2. NOT formal: no Lean checker and no theorem are involved.",
      "arguments": {
        "decision_id": {"type": "string", "required": true,
                        "help": "Caller's identifier for this decision, bound into the certificate. Bytes are restricted to [A-Za-z0-9_]; anything else refuses `prog-symbol`."},
        "criterion":   {"type": "string", "required": true,
                        "help": "The declared numeric criterion (e.g. 'latency_ms'); a value judgment refuses `decision-value-judgment`."},
        "sense":       {"type": "string", "required": true,
                        "help": "Exactly 'min' or 'max'; anything else refuses `decision-sense-unknown`."},
        "options":     {"type": "string", "required": true,
                        "help": "2..6 whitespace-separated `label value` pairs, e.g. 'a 10 b 20 c 30'. Values are canonical integers. A label containing whitespace shifts the pairing and refuses `pack-args-shape`; it is never silently re-paired."}
      },
      "returns": {
        "status": "exact | refused",
        "assurance_ceiling": "exact",
        "consequence_ceiling": "decision-boundary",
        "checker_rerun": "ACCEPT on success (tools/decision_verify.py, digest pinned by the pack manifest)",
        "certificate": "the verbatim jackal-decision-cert-v1 envelope line",
        "fields": "selected=, margin=, consequence=, note=",
        "identities": "{registry_file_sha256, registry_digest_sha256, pack_verifier_sha256, pack_manifest_sha256, evaluator_sha256, checker_sha256, plugin_sha256}",
        "reason": "stable refusal class on refusal (decision-value-judgment, decision-sense-unknown, decision-duplicate-label, decision-margin-zero, pack-request-arity, pack-args-shape, checker-rejected, ...)",
        "detail": "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_decision_rank_v2",
      "description": "Domain pack jackal.decision.matrix, operation decision.matrix.rank.v2, routed through pack-route. Same deterministic ordering as jackal_decision_rank, plus a REQUIRED declared unit drawn from a closed vocabulary: the 65 canonical ids of release/claim/unit_registry_v1.json excluding the dimensionless identity `one` (66 ids in the registry, `one` is not admitted here). Matching is exact-token and case-sensitive; an alias, a spelled-out name such as 'millisecond', a different case, or the dimensionless identity all refuse `decision-unit-unknown`, and an empty unit refuses `decision-unit-missing`. Emits jackal-decision-cert-v2, re-checked by the manifest-pinned tools/decision_verify.py; only ACCEPT returns success. ASSURANCE ceiling exact; CONSEQUENCE ceiling decision-boundary. Honest residual: a declared unit is NOT a measurement. The closed vocabulary forces the caller to name a dimension, and nothing more — a value-judgment criterion that survives the engine's word list is still accepted when a real unit is declared (`most_elegant` in `ms` ranks), and the values themselves remain caller-declared. NOT formal: no Lean checker and no theorem are involved.",
      "arguments": {
        "decision_id": {"type": "string", "required": true,
                        "help": "Caller's identifier for this decision, bound into the certificate. Bytes are restricted to [A-Za-z0-9_]; anything else refuses `prog-symbol`."},
        "criterion":   {"type": "string", "required": true,
                        "help": "The declared numeric criterion; a value judgment refuses `decision-value-judgment` even with an admissible unit."},
        "unit":        {"type": "string", "required": true,
                        "help": "One canonical unit id from release/claim/unit_registry_v1.json, excluding `one`. Exact token, case-sensitive: 'ms' is admitted, 'millisecond' and 'MS' refuse `decision-unit-unknown`."},
        "sense":       {"type": "string", "required": true,
                        "help": "Exactly 'min' or 'max'; anything else refuses `decision-sense-unknown`."},
        "options":     {"type": "string", "required": true,
                        "help": "2..6 whitespace-separated `label value` pairs, e.g. 'a 10 b 20 c 30'. Values are canonical integers. A label containing whitespace shifts the pairing and refuses `pack-args-shape`; it is never silently re-paired."}
      },
      "returns": {
        "status": "exact | refused",
        "assurance_ceiling": "exact",
        "consequence_ceiling": "decision-boundary",
        "checker_rerun": "ACCEPT on success (tools/decision_verify.py, digest pinned by the pack manifest)",
        "certificate": "the verbatim jackal-decision-cert-v2 envelope line",
        "fields": "selected=, margin=, unit=, consequence=, note=",
        "identities": "{registry_file_sha256, registry_digest_sha256, pack_verifier_sha256, pack_manifest_sha256, evaluator_sha256, checker_sha256, plugin_sha256}",
        "non_claims": "includes a_declared_unit_is_not_a_measurement",
        "reason": "stable refusal class on refusal (decision-unit-missing, decision-unit-unknown, decision-value-judgment, decision-sense-unknown, decision-duplicate-label, decision-margin-zero, pack-request-arity, pack-args-shape, checker-rejected, ...)",
        "detail": "short human-readable string on refusal"
      }
    },
    {
      "name": "jackal_anubis_check_program",
      "description": "Run a caller-pinned Anubis compiler only as `build --evidence` in Safe mode, never execute the compiled artifact, then independently close and replay the resulting strict anubis.program-evidence.v3 package under inventory-safe-v1. Emits only verified-program-evidence or a named refusal. This profile checks producer-attested function/policy inventories but explicitly does not establish policy-construct totality, source-to-VC proof, SMT-to-CNF proof, source-native refinement, runtime behavior, or universal soundness.",
      "arguments": {
        "source_path": {"type": "string", "required": true, "help": "Local regular non-symlink Anubis source path."},
        "anubis_bin": {"type": "string", "required": true, "help": "Local regular non-symlink Anubis executable; caller-pinned and hashed before/after build."},
        "expected_source_sha256": {"type": "string", "required": true, "help": "Caller-pinned exact source SHA-256."},
        "expected_compiler_sha256": {"type": "string", "required": true, "help": "Caller-pinned approved Anubis executable SHA-256."},
        "expected_policy_sha256": {"type": "string", "required": true, "help": "Caller-pinned inventory-safe-v1 policy digest."},
        "verification_time_unix": {"type": "string", "required": true, "help": "Caller-supplied Unix verification time bound into the receipt."},
        "profile": {"type": "string", "required": true, "help": "Must be inventory-safe-v1; contracted-safe-v1 refuses."},
        "nonce": {"type": "string", "required": true, "help": "Caller nonce bound into the receipt."},
        "out_root": {"type": "string", "required": true, "help": "New local output root; existing paths refuse."}
      },
      "returns": {
        "status": "verified-program-evidence | refused",
        "receipt": "jackal-anubis-program-receipt-v1 on success",
        "residual_non_claims": "includes policy-construct-totality-not-established and every source/proof/runtime residual",
        "reason": "stable refusal reason",
        "detail": "bounded refusal detail"
      }
    },
    {
      "name": "jackal_anubis_verify_program",
      "description": "Independently verify caller-selected Anubis Safe source and evidence bytes under inventory-safe-v1: strict v3 roster, exact manifest closure, source/compiler/artifact/policy pins, producer-summary reconciliation, one-to-one solver/proof paths and counters, approved Z3 UNSAT replay, and independent RUP replay. Never executes the artifact and never claims independent policy-construct totality or source-native refinement.",
      "arguments": {
        "source_path": {"type": "string", "required": true, "help": "Caller-selected regular non-symlink Anubis source path."},
        "evidence_dir": {"type": "string", "required": true, "help": "Caller-selected strict anubis.program-evidence.v3 directory."},
        "expected_source_sha256": {"type": "string", "required": true, "help": "Caller-pinned exact source SHA-256."},
        "expected_compiler_sha256": {"type": "string", "required": true, "help": "Caller-pinned producer executable SHA-256."},
        "expected_artifact_sha256": {"type": "string", "required": true, "help": "Caller-pinned sealed artifact SHA-256; artifact bytes are read but never executed."},
        "expected_policy_sha256": {"type": "string", "required": true, "help": "Caller-pinned inventory-safe-v1 policy digest."},
        "verification_time_unix": {"type": "string", "required": true, "help": "Caller-supplied Unix verification time bound into the receipt."},
        "profile": {"type": "string", "required": true, "help": "Must be inventory-safe-v1."},
        "nonce": {"type": "string", "required": true, "help": "Caller nonce bound into the receipt."}
      },
      "returns": {
        "status": "verified-program-evidence | refused",
        "receipt": "jackal-anubis-program-receipt-v1 on success",
        "residual_non_claims": "strict residual roster including policy-construct-totality-not-established",
        "reason": "stable refusal reason",
        "detail": "bounded refusal detail"
      }
    },
    {
      "name": "jackal_anubis_verify_program_receipt",
      "description": "Recompute a jackal-anubis-program-receipt-v1 from caller-selected source/evidence bytes and independent caller pins, rejecting outer-digest-consistent semantic laundering. Success is only verified-program-receipt; it does not raise the underlying inventory-safe-v1 assurance ceiling.",
      "arguments": {
        "receipt": {"type": "object", "required": true, "help": "Program receipt object to replay; its own pins are never trusted."},
        "source_path": {"type": "string", "required": true, "help": "Caller-selected regular source path."},
        "evidence_dir": {"type": "string", "required": true, "help": "Caller-selected evidence directory."},
        "expected_source_sha256": {"type": "string", "required": true, "help": "Caller-pinned source SHA-256."},
        "expected_compiler_sha256": {"type": "string", "required": true, "help": "Caller-pinned compiler SHA-256."},
        "expected_artifact_sha256": {"type": "string", "required": true, "help": "Caller-pinned artifact SHA-256."},
        "expected_policy_sha256": {"type": "string", "required": true, "help": "Caller-pinned inventory-safe-v1 policy digest."},
        "verification_time_unix": {"type": "string", "required": true, "help": "Caller-supplied Unix verification time bound into the receipt."},
        "profile": {"type": "string", "required": true, "help": "Must be inventory-safe-v1."},
        "nonce": {"type": "string", "required": true, "help": "Caller nonce; never copied from the receipt."}
      },
      "returns": {
        "status": "verified-program-receipt | refused",
        "receipt": "fully recomputed receipt on success",
        "residual_non_claims": "identical to the recomputed inventory-safe-v1 receipt",
        "reason": "stable refusal reason",
        "detail": "bounded refusal detail"
      }
    }
  ]
}
